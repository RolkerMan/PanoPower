实测需要https 才能使用安装 app 而不是添加快捷方式到主屏幕。
如果是添加快捷方式到主屏幕，打开的时候不会全屏显示，依然是浏览器界面。

参考 AI 助手的回答：
出现这种情况的原因是PWA（渐进式Web应用）“安装到主屏幕”功能和“添加快捷方式”有本质区别，且有明确的标准规范：

必须HTTPS：根据W3C的PWA规范，只有在HTTPS环境下，浏览器才允许“安装”PWA（即以接近原生App的方式全屏运行、独立于浏览器UI）。HTTP下只能“添加到主屏幕”，但只是快捷方式，依然以浏览器壳打开。

相关标准文档：

W3C PWA规范（Web App Manifest）：https://www.w3.org/TR/appmanifest/
Service Worker规范（要求HTTPS）：https://w3c.github.io/ServiceWorker/#security-considerations
主要原因：

安全性：Service Worker和PWA安装涉及离线缓存、推送等能力，必须在HTTPS下保证安全。
浏览器策略：只有满足manifest、service worker、HTTPS等条件，才会出现“安装”按钮，且安装后可全屏、无浏览器UI。
结论：这是浏览器和Web标准的明确要求，属于规范行为。

参考资料：
主屏幕 app
https://www.cnblogs.com/miangao/p/15128769.html

PWA webapp
https://juejin.cn/post/7516122409948020736

添加到桌面，实例，可以全屏：
https://github.com/tecdrop/pwa-display-test